# Build directories
llvm-build/
out_tree_pass/build/

# Various constructed binaries and LLVM IR 
rand_instructions/objects/
objects/

# Caches, autogenerated, and venv
__pycache__/
.cache/
.venv/
old_files/

*.tar.gz

# Submodule (should be excluded anyway?)
McPAT_monolithic/

# Exclude mcpat_inputs since full of generated files, but keep the base file we adjust to generate our inputs
mcpat_inputs/
!mcpat_inputs/Alpha21364.xml

# Exclude mcpat_out which is full of output files
mcpat_out/
